% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_dragen_cnv_vcf.R
\name{read_dragen_cnv_vcf}
\alias{read_dragen_cnv_vcf}
\title{Read and parse DRAGEN CNV VCF files}
\usage{
read_dragen_cnv_vcf(
  vcf_file = NULL,
  vcf_dir = NULL,
  suffix = "vcf,vcf.gz",
  recursive = FALSE,
  out_dir = NULL,
  out_type = "All_CNV",
  out_sep = "\t",
  out_col = FALSE
)
}
\arguments{
\item{vcf_file}{Character. Path to one VCF or VCF.GZ file. Mutually
exclusive with \code{vcf_dir}.}

\item{vcf_dir}{Character. Path to a directory containing VCF files.
When set, every file matching \code{suffix} (optionally recursively) is
read and combined.}

\item{suffix}{Character. Comma-separated list of acceptable file suffixes.
Default \code{"vcf,vcf.gz"}. Substrings are matched with \code{endsWith()}
so a custom value like \code{"cnv.vcf.gz"} works too.}

\item{recursive}{Logical. When \code{vcf_dir} is set, whether to recurse
into sub-directories. Default \code{FALSE}.}

\item{out_dir}{Character or \code{NULL}. When set, the chosen table(s)
(see \code{out_type}) are written to this directory using
\code{utils::write.table()}. The directory is created if it does not
exist. Default \code{NULL} (no files written).}

\item{out_type}{Character vector. Which table(s) to write when
\code{out_dir} is set. Case-insensitive; accepts \code{"All_CNV"} /
\code{"all"}, and \code{"Passed_CNV"} / \code{"passed"} / \code{"pass"}.
Multiple values are allowed. Default \code{"All_CNV"}. Unknown values
raise an error.}

\item{out_sep}{Character. Field separator passed to \code{write.table()}
when writing output. Default \code{"\t"}. If set to \code{","} the file
extension switches to \code{.csv}, otherwise \code{.tsv}.}

\item{out_col}{Logical. Value of \code{col.names} passed to
\code{write.table()} when writing output. Default \code{FALSE}.}
}
\value{
If \code{vcf_file} is supplied, a \code{list} with three named elements:
\describe{
  \item{\code{Passed_CNV}}{data.frame: chr, start, end, cnv, CN, sample_id;
        filtered to FILTER == "PASS" and ALT in DEL/DUP/LOH (i.e.
        actual CNV events; REF segments and INV/INS/BND structural
        variants are excluded — they remain in All_CNV).}
  \item{\code{All_CNV}}{data.frame: all CNV records with the FORMAT fields
        expanded into separate columns (GT, CN, MCN, CNQ, MCNQ, CNF,
        MCNF, MF, SM, SD, MAF, BC, AS, PE) plus QUAL, FILTER, ALT,
        sample_id.}
  \item{\code{format_lines}}{The raw ##FORMAT=... lines from the VCF header.}
}

If \code{vcf_dir} is supplied, a \code{list} whose elements are the
per-sample results above, plus an additional element \code{all}
containing the concatenated \code{Passed_CNV}, \code{All_CNV} and the
per-sample \code{format_lines}.
}
\description{
Reads CNV (copy-number variation) VCF files produced by the Illumina
DRAGEN pipeline, parses the multi-key FORMAT field into separate
columns, normalises the DRAGEN ALT codes into a tidy \code{cnv}
category, and returns both the PASS-only and the full call set as
data frames. Optionally writes the chosen table to disk.

Single-file and recursive batch modes are supported.
}
\details{
The FORMAT/sample column is split on \code{:} into the keys named in the
FORMAT string, and pivoted into separate columns. Numeric-like columns
(CN, MCN, CNQ, MCNQ, MF, SM, SD, MAF, BC, AS) are coerced to numeric.

ALT values are stripped of \code{<>} brackets and validated against the
supported set \code{.} (DRAGEN's REF segment marker), \code{DEL},
\code{DUP}, \code{LOH}, \code{REF}, \code{INV}, \code{INS},
\code{BND}. Any other value raises an error. Supported ALTs are
mapped to the \code{cnv} category:
\itemize{
  \item \code{DEL} -> \code{loss}
  \item \code{DUP} -> \code{gain}
  \item \code{LOH} with \code{CN == 2} -> \code{cn-LOH}
  \item \code{LOH} otherwise -> \code{LOH}
  \item \code{REF} or \code{.} -> \code{ref}
  \item \code{INV} -> \code{inv}
  \item \code{INS} -> \code{ins}
  \item \code{BND} -> \code{bnd}
}
}
\examples{
\dontrun{
# Single VCF
res <- read_dragen_cnv_vcf(vcf_file = "sample.cnv.vcf.gz")
head(res$Passed_CNV)
head(res$All_CNV)

# All VCFs in a folder (non-recursive)
res <- read_dragen_cnv_vcf(vcf_dir = "cnv_vcfs/", recursive = FALSE)
head(res$all$Passed_CNV)

# Custom suffix, recursive
res <- read_dragen_cnv_vcf(
  vcf_dir   = "cnv_vcfs/",
  suffix    = "cnv.vcf.gz",
  recursive = TRUE
)

# Write per-sample All_CNV tables to disk as TSV
read_dragen_cnv_vcf(
  vcf_dir  = "cnv_vcfs/",
  out_dir  = "cnv_tsv/",
  out_type = "All_CNV"
)

# Write both All_CNV and Passed_CNV as CSV, with a header row
read_dragen_cnv_vcf(
  vcf_dir  = "cnv_vcfs/",
  out_dir  = "cnv_csv/",
  out_type = c("All_CNV", "pass"),
  out_sep  = ",",
  out_col  = TRUE
)
}
}
